# Copy this file to .env for local testing (Railway uses its own Variables tab instead).

# Shared secret the laptop script must send in the "x-api-key" header.
# Leave blank while testing locally; set a real value before deploying publicly.
API_KEY=change-me-to-a-long-random-string

# Railway sets PORT automatically in production - only needed for local runs.
PORT=3000
